import { createClient } from '@supabase/supabase-js';

const supabase = createClient(
  'https://xtxreonllniybgockvki.supabase.co',
  'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...' // I don't have the anon key!
);
