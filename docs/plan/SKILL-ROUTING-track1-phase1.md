# SKILL ROUTING: Track 1 - Phase 1 (Word Count Engine)

### Được phép sử dụng:
- **TypeScript thuần:** Xây dựng domain functions, types và interfaces trong `src/domain/wordCount.ts`.
- **Vitest:** Viết và chạy unit tests trong `src/domain/wordCount.test.ts`.

### KHÔNG được phép sử dụng:
- Không đụng chạm vào UI components (`components/ControlPanel.tsx`, `components/OutputDisplay.tsx`...) trong Phase 1 (sẽ làm ở Phase 4).
- Không sửa file prompts (`src/services/ai/prompts/index.ts`) trong Phase 1 (sẽ làm ở Phase 2).
- Không sửa workflow (`src/features/generation/useGenerationWorkflow.ts`) trong Phase 1 (sẽ làm ở Phase 3).
- Không cài thêm dependencies npm bên ngoài nếu không thực sự cần thiết (sử dụng Vitest có sẵn trong repo).
