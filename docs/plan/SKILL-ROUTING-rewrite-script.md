# SKILL ROUTING: Rewrite Script

**Được phép sử dụng:**
- Typescript (để sửa React components, AI hooks, và API).
- Zod/Zustand (nếu cần quản lý state, mặc dù tính năng này đơn giản có thể dùng React useState).
- Tailwind CSS (để build giao diện Modal Side-by-side).

**KHÔNG được phép sử dụng:**
- Không thay đổi các luồng Deep Research hiện có.
- Không tự ý sinh thêm Prompt AI mới (chỉ tái sử dụng `finance.script.revise` và chèn thêm block ép cấu trúc 5 phần).
