# SKILL ROUTING: Track 1 - Phase 4 (UI Components)

### Được phép sử dụng:
- **React & Tailwind CSS:** Chỉnh sửa UI trong `components/ControlPanel.tsx`, `components/OutputDisplay.tsx`, và `App.tsx`.
- **Domain Imports:** Import các hàm định dạng và kiểm tra từ `src/domain/wordCount.ts` (`countWords`, `getWordCountTolerance`, `isWithinTolerance`, `formatWordCount`, `determineToleranceMode`).

### KHÔNG được phép sử dụng:
- Không thay đổi các quy tắc trong Prompt Registry hay Logic Deep Research.
- Không phá vỡ theme Tailwind Dark Mode (Emerald / Zinc) hiện hữu của ứng dụng.
