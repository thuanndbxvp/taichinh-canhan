# SKILL ROUTING: Track 2 - Phase 5 (Full Persistence & Verification)

### Được phép sử dụng:
- **Supabase Client:** Viết các truy vấn Insert/Update/Delete trong `NicheService.ts`.
- **Testing & Verification:** Chạy Vitest, TypeScript compiler (`npm run typecheck`), và Vite production build (`npm run build`).

### KHÔNG được phép sử dụng:
- Tuyệt đối cấm thao tác xóa/sửa đối với các Niche hệ thống (`is_system = true`).
